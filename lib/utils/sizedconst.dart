//build height
import 'package:flutter/material.dart';

SizedBox buildHeight(double height) => SizedBox(
      height: height,
    );

//build width
SizedBox buildWidth(double width) => SizedBox(
      width: width,
    );
